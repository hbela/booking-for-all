module.exports = {
    dependencies: {},
    project: {
      android: {
        sourceDir: "./android",
      },
    },
    reactNativePath: rmodule.exports = {
  dependencies: {},
  project: {
    android: {
      sourceDir: "./android",
    },
  },
  reactNativePath: require.resolve("react-native"),
  nodeModulesPath: [
    // monorepo workspace roots where native deps located
    require("path").join(__dirname, "../../node_modules"),
    require("path").join(__dirname, "node_modules"),
  ],
};
equire.resolve("react-native"),
    nodeModulesPath: [
      // monorepo workspace roots where native deps located
      require("path").join(__dirname, "../../node_modules"),
      require("path").join(__dirname, "node_modules"),
    ],
  };
  